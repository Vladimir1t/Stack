#include "StackPush.h"

void StackPush (struct Stack* stP, const int numberPush)
{
    if (st->size == st->capacity)
    {
        stP->data = (int*) realloc (st->data, (st->size * 2) * sizeof(int));
        stP->capacity *= 2;
    }

    stP->data[st->size] = numberPush;
    printf ("StackPush(): element = %d\n", stP->data[st->size]);

    stP->size++;

    printf ("StackPush(): number has been pushed successfully\n");
}
